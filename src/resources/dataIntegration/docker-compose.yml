version: '3.8'

services:
  my_container:
    build:
      context: .
      dockerfile: DockerfileHistoric
    volumes:
      - ./data:/data
    networks:
      - acquaount-platform_default

networks:
  acquaount-platform_default:
    external: true
